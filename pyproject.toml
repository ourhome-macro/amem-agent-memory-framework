[build-system]
requires = ["hatchling>=1.25"]
build-backend = "hatchling.build"

[project]
name = "agent-memory-runtime"
version = "0.4.0"
description = "Durable business-agent orchestration and event-sourced memory runtime."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "Agent Memory Runtime Contributors" }]
dependencies = [
  "openai>=1.0",
  "python-dotenv>=1.0",
  "typer>=0.12",
  "rich>=13.7",
  "PyYAML>=6.0",
]

[project.optional-dependencies]
dev = [
  "pytest>=8.0",
  "ruff>=0.6",
]

[project.scripts]
amem = "agent_memory_runtime.cli.app:app"

[tool.hatch.build.targets.wheel]
packages = ["src/agent_memory_runtime"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
